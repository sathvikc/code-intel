{
  "name": "code-intel",
  "version": "0.11.3",
  "description": "Static analyzer that finds the coupling your import graph can't see.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "code-intel": "src/cli.js"
  },
  "scripts": {
    "test": "node --test tests/*.test.js scripts/*.test.js",
    "commit": "./scripts/commit.sh",
    "postinstall": "git config core.hooksPath .githooks || true"
  },
  "dependencies": {
    "typescript": "^5.6.0"
  }
}
