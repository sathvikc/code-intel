// Committed vendor copy — should be skipped (ancestor dir: vendor/).
// Pretend this is a 300KB library file.
function createElement(type, props) { return { type, props }; }
localStorage.setItem('vendor-noise', '1');
